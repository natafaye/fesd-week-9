

function filterByFirstLetter(array, letter) {
    return array.filter(item => item.startsWith(letter))
}

// I'd make sure it works with a bunch of names and the letter M
// make sure it works with an empty list